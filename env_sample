# Copy this file to ".env" and replace placeholder values with real secrets.
# Keep the file out of version control (add to .gitignore).

# Django secret key
SECRET_KEY=REPLACE_WITH_YOUR_SECRET_KEY

# Debug mode (True/False)
DEBUG=False

# Hosts (comma-separated)
ALLOWED_HOSTS=localhost,127.0.0.1,api.sdgpss-inweh.unu.edu

# Database
DATABASE_NAME=sdgpss
DATABASE_USER=sdgpss
DATABASE_PASSWORD=REPLACE_WITH_DATABASE_PASSWORD
DATABASE_HOST=127.0.0.1
DATABASE_PORT=5432

# Email (SMTP)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=REPLACE_WITH_EMAIL_ADDRESS
EMAIL_HOST_PASSWORD=REPLACE_WITH_EMAIL_PASSWORD

# CORS whitelist (comma-separated origins)
CORS_ORIGIN_WHITELIST=https://sdgpss-inweh.unu.edu,http://localhost:4201

# Admin contact (used for error emails)
ADMIN_NAME=PSS Admin
ADMIN_EMAIL=REPLACE_WITH_ADMIN_EMAIL
SERVER_EMAIL=REPLACE_WITH_SERVER_EMAIL
